pr:
  branches:
    include:
     - "*"

trigger:
  branches:
    exclude:
    - "*"
  
pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    cmake .
  displayName: 'Generate MakeFiles'

- script: |
    make
  displayName: 'Build Project'
